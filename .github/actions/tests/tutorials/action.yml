name: "Run tutorial tests"

description: 

runs:
  using: composite
  steps:
    - name: Run turorial tests
      shell: bash
      run: |
        pwd
        ls -1a
        which python
        jupyter nbconvert --to notebook --execute tutorials/*.ipynb
