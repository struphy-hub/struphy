name: "Clone and install struphy"

description: 

runs:
  using: composite
  steps:

    - name: Install struphy
      shell: bash
      run: |
        pip install --upgrade pip
        pip uninstall -y gvec
        pip install -U --upgrade-strategy eager -e ".[dev,mpi,doc]"
        pip list
        struphy -h
