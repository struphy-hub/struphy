name: Publish Struphy to PyPI

on:
  push:
    branches:
      - main

# .github/workflows/ci-cd.yml
jobs:
  pypi-publish:
    name: Upload release to PyPI

    runs-on: ubuntu-latest

    environment:
      name: pypi
      url: https://pypi.org/project/struphy/

    permissions:
      id-token: write  # IMPORTANT: this permission is mandatory for trusted publishing

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        # browser/build.py uses tomllib and emits the pure-Python Pyodide wheel.
        python-version: "3.11"

    - name: Install build tools
      run: |
        python -m pip install --upgrade pip
        pip install build twine

    - name: Build the package
      run: python -m build

    - name: Build the Pyodide-compatible geometry package
      run: python browser/build.py --output dist

    - name: Show release distributions
      run: ls -l dist/

    - name: Publish package distributions to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      # with:
        # repository-url: https://test.pypi.org/legacy/
