name: Create pull request into struphy-hub/struphy-tutorials

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  preflight:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      should_run: ${{ steps.filter.outputs.should_run }}
      reason: ${{ steps.filter.outputs.reason }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          fetch-depth: 1
          ref: ${{ github.event_name == 'pull_request' && format('refs/pull/{0}/head', github.event.number) || github.sha }}

      - name: Check workflow filters
        id: filter
        uses: ./.github/actions/commit-message-filter
        with:
          categories: tutorials
          event_name: ${{ github.event_name }}

  clone-push-and-create-pr:
    needs: preflight
    if: needs.preflight.outputs.should_run == 'true'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: true  # This is crucial!
          fetch-depth: 5

      - name: Read Struphy version from pyproject
        uses: ./.github/actions/struphy-version

      - name: Checkout code
        uses: actions/checkout@v4
        with:
          repository: struphy-hub/struphy-tutorials
          token: ${{ secrets.STRUPHY_HUB_ACCESS_TOKEN }}
          path: struphy-tutorials

      - name: Copy tutorials and push new branch
        run: |
            cd struphy-tutorials
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git checkout -b from-struphy-${{ github.ref_name }}-${{ github.event.number }}-${{ github.run_number }}
            ls -a
            rm ./tutorial*
            ls -a
            cp -r ../tutorials/. ./
            git status
            git add .
            git commit -m "Changes to tutorials from struphy (on push to main)"
            git push origin from-struphy-${{ github.ref_name }}-${{ github.event.number }}-${{ github.run_number }}

      - name: Create version tag in struphy-tutorials
        run: |
            cd struphy-tutorials
            TAG_NAME="v${STRUPHY_VERSION}"
            if git ls-remote --exit-code --tags origin "refs/tags/${TAG_NAME}" > /dev/null; then
              echo "Tag ${TAG_NAME} already exists in origin. Skipping tag creation."
            else
              git tag -a "${TAG_NAME}" -m "Tutorials for Struphy v${STRUPHY_VERSION}"
              git push origin "${TAG_NAME}"
            fi

      - name: Create pull request
        run: |
          gh pr create \
            --repo struphy-hub/struphy-tutorials \
            --title "New tutorials from struphy v${STRUPHY_VERSION}" \
            --body "This is an auto-generated PR from a Struphy workflow (on push to main)." \
            --head from-struphy-${{ github.ref_name }}-${{ github.event.number }}-${{ github.run_number }} \
            --base main 
        env:
          GH_TOKEN: ${{ secrets.STRUPHY_HUB_ACCESS_TOKEN }}