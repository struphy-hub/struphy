on:
  workflow_call:
    inputs:
      os:
        required: true
        type: string

jobs:
  hello_world:
    runs-on: ${{ inputs.os }}
    steps:
      - name: Say hello
        run: echo "Hello world!"